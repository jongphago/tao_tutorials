random_seed: 42
dataset_config {
  data_sources {
    tfrecords_path: "/workspace/tao-experiments/data/tfrecords/kitti_trainval/*"
    image_directory_path: "/workspace/tao-experiments/data/training"
  }
  image_extension: "jpg"                    # 변경: png → jpg (실제 데이터 형식에 맞춤)
  target_class_mapping {
    key: "person"
    value: "person"                         # 변경: 단일 클래스 person만 사용 (AIHUB KAIST 데이터)
  }
  validation_fold: 0
}
augmentation_config {
  preprocessing {
    output_image_width: 1248                # 유지: KITTI 원본 해상도 (성능 검증된 크기)
    output_image_height: 384                # 유지: KITTI 원본 해상도
    min_bbox_width: 1.0                     # 유지: 작은 객체도 감지 가능
    min_bbox_height: 1.0                    # 유지: 작은 객체도 감지 가능
    output_image_channel: 3
  }
  spatial_augmentation {
    hflip_probability: 0.5                  # 유지: 수평 뒤집기로 데이터 증강
    zoom_min: 0.9                          # 변경: 1.0 → 0.9 (약간의 확대/축소 추가)
    zoom_max: 1.1                          # 변경: 1.0 → 1.1 (약간의 확대/축소 추가)
    translate_max_x: 12.0                   # 변경: 8.0 → 12.0 (이동 범위 증가)
    translate_max_y: 12.0                   # 변경: 8.0 → 12.0 (이동 범위 증가)
  }
  color_augmentation {
    hue_rotation_max: 25.0                  # 유지: 색상 증강
    saturation_shift_max: 0.20000000298     # 유지: 채도 증강
    contrast_scale_max: 0.10000000149       # 유지: 대비 증강
    contrast_center: 0.5                    # 유지: 대비 중심점
  }
}
postprocessing_config {
  target_class_config {
    key: "person"
    value {
      clustering_config {
        clustering_algorithm: DBSCAN
        dbscan_confidence_threshold: 0.9    # 유지: 원본 KITTI 높은 신뢰도 (0.9)
        coverage_threshold: 0.00749999983236 # 차용: pedestrian 설정값 (가장 유사한 클래스)
        dbscan_eps: 0.230000004172          # 차용: pedestrian 설정값 (person과 가장 유사)
        dbscan_min_samples: 1               # 유지: 원본 KITTI 설정
        minimum_bounding_box_height: 20     # 유지: 최소 높이 20픽셀
      }
    }
  }
}
model_config {
  pretrained_model_file: "/workspace/tao-experiments/detectnet_v2/pretrained_resnet18/pretrained_detectnet_v2_vresnet18/resnet18.hdf5"
  num_layers: 18                            # 유지: ResNet18 아키텍처
  use_batch_norm: true                      # 유지: 배치 정규화 사용
  objective_set {
    bbox {
      scale: 35.0                           # 유지: 바운딩 박스 손실 스케일
      offset: 0.5                           # 유지: 바운딩 박스 오프셋
    }
    cov {
    }
  }
  arch: "resnet"                           # 유지: ResNet 아키텍처
}
evaluation_config {
  validation_period_during_training: 10     # 유지: 10 에포크마다 검증
  first_validation_epoch: 30               # 유지: 30 에포크부터 검증 시작 (충분한 학습 후)
  minimum_detection_ground_truth_overlap {
    key: "person"
    value: 0.5                             # 차용: pedestrian IoU 임계값 (0.5)
  }
  evaluation_box_config {
    key: "person"
    value {
      minimum_height: 20                    # 유지: 최소 높이 20픽셀
      maximum_height: 9999                  # 유지: 최대 높이 제한 없음
      minimum_width: 10                     # 유지: 최소 너비 10픽셀
      maximum_width: 9999                   # 유지: 최대 너비 제한 없음
    }
  }
  average_precision_mode: INTEGRATE         # 유지: 적분 방식 mAP 계산
}
cost_function_config {
  target_classes {
    name: "person"
    class_weight: 4.0                       # 차용: pedestrian 클래스 가중치 (4.0)
    coverage_foreground_weight: 0.0500000007451 # 유지: coverage 손실 가중치
    objectives {
      name: "cov"
      initial_weight: 1.0                   # 유지: coverage 손실 초기 가중치
      weight_target: 1.0                    # 유지: coverage 손실 목표 가중치
    }
    objectives {
      name: "bbox"
      initial_weight: 10.0                  # 유지: bbox 손실 초기 가중치
      weight_target: 10.0                   # 유지: bbox 손실 목표 가중치
    }
  }
  enable_autoweighting: false               # 유지: 자동 가중치 조정 비활성화
  max_objective_weight: 0.999899983406      # 유지: 최대 목적 함수 가중치
  min_objective_weight: 9.99999974738e-05  # 유지: 최소 목적 함수 가중치
}
training_config {
  batch_size_per_gpu: 4                     # 유지: GPU당 배치 크기 4
  num_epochs: 150                          # 변경: 120 → 150 (더 충분한 학습)
  learning_rate {
    soft_start_annealing_schedule {
      min_learning_rate: 5e-07              # 유지: 최소 학습률
      max_learning_rate: 1e-04              # 변경: 5e-05 → 1e-04 (2배 증가, 안정적 범위)
      soft_start: 0.10000000149             # 유지: soft start 비율
      annealing: 0.699999988079             # 유지: annealing 비율
    }
  }
  regularizer {
    type: L1                                # 유지: L1 정규화
    weight: 3.00000002618e-09               # 유지: 정규화 가중치
  }
  optimizer {
    adam {
      epsilon: 9.99999993923e-09            # 유지: Adam 엡실론
      beta1: 0.899999976158                 # 유지: Adam beta1
      beta2: 0.999000012875                 # 유지: Adam beta2
    }
  }
  cost_scaling {
    initial_exponent: 20.0                  # 유지: 초기 비용 스케일링
    increment: 0.005                        # 유지: 증가량
    decrement: 1.0                          # 유지: 감소량
  }
  visualizer{
    enabled: true                           # 유지: 시각화 활성화
    num_images: 3                           # 유지: 시각화 이미지 수
    scalar_logging_frequency: 50            # 유지: 스칼라 로깅 빈도
    infrequent_logging_frequency: 5         # 유지: 이미지 로깅 빈도
    target_class_config {
      key: "person"
      value: {
        coverage_threshold: 0.005           # 유지: 시각화 coverage 임계값
      }
    }
    clearml_config{
      project: "TAO Toolkit ClearML Demo"
      task: "detectnet_v2_resnet18_clearml_final"
      tags: "detectnet_v2"
      tags: "training"
      tags: "resnet18"
      tags: "unpruned"
      tags: "final"
    }
    wandb_config{
      project: "TAO Toolkit Wandb Demo"
      name: "detectnet_v2_resnet18_wandb_final"
      tags: "detectnet_v2"
      tags: "training"
      tags: "resnet18"
      tags: "unpruned"
      tags: "final"
    }
  }
  checkpoint_interval: 10                   # 유지: 10 에포크마다 체크포인트 저장
}
bbox_rasterizer_config {
  target_class_config {
    key: "person"
    value {
      cov_center_x: 0.5                     # 유지: coverage 중심 X
      cov_center_y: 0.5                     # 유지: coverage 중심 Y
      cov_radius_x: 1.0                     # 차용: pedestrian 설정 (1.0)
      cov_radius_y: 1.0                     # 차용: pedestrian 설정 (1.0)
      bbox_min_radius: 1.0                  # 유지: 최소 바운딩 박스 반지름
    }
  }
  deadzone_radius: 0.400000154972           # 유지: deadzone 반지름
}
